model:
  image_size: 28
  in_channels: 1
  base_channels: 64

diffusion:
  timesteps: 1000
  beta_start: 0.0001
  beta_end: 0.02

training:
  batch_size: 64
  epochs: 10
  learning_rate: 1e-4
  device: "cuda"